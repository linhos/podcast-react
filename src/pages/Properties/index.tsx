import React from 'react';

interface Props {
    name: string
}

const Properties = ({ name }: Props): React.ReactNode => {
    return(
        <>
            <h1>Properties</h1>
        </>
    )
}

export default Properties;